#include <iostream>

/*
const int* const myClassMethod(const int* const & param) const;
*const 表示这是一个const pointer, always points to the same address
const int* const表示这个const pointer指向一个const int
a const pointer which always points to the same address, and the contents in the address won't change
函数结尾的const用于表示this is a const member function that cannot modify any variables of the this instance
*/
int main() {
    
    return 0;
}